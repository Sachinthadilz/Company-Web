// This file MUST be imported first in server.ts.
// Node.js runs imports in order, so dotenv.config() here
// populates process.env before any other module reads it.
import dotenv from 'dotenv';
dotenv.config();
